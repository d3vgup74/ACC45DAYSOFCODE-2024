T = int(input())

while T:
    N, K = map(int, input().split())
    x = input().split(" ")
    ans = 0
    
    for i in range(len(x)):
        if (int(x[i]) + K) % 7 == 0:
            ans += 1
    
    print(ans)
    
    T -= 1
